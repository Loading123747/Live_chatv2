# Live_chatv2
this a better version of the old "Live_chat" to deploy can fork this repository then go to code sandbox. then click import github repo. then select your file. go to terminal then create a new terminal and put in these commands: pip install flask flask-socketio. click enter then put in this command: python main.py
